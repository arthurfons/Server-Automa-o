services:
  - type: web
    name: google-ads-automation
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run streamlit_app_final_limpo.py --server.port=$PORT --server.address=0.0.0.0
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.7 